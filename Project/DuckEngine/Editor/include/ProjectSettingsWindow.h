/****************************************************************************/
/*!
\file   ProjectSettingsWindow.h
\author [Your Name]
\brief  A small ImGui window that modifies project settings (stored in ProjectSettings).

This file belongs to the LevelEditor.
*/
/****************************************************************************/
#pragma once

class ProjectSettingsWindow
{
public:
	// Show the ImGui window if `open == true`
	static void Show(bool& open);
};
